[tool.ruff]
target-version = "py310"
# Honour the excludes below even when a file is passed explicitly on the
# command line. Ruff's default is to lint anything named directly, so without
# this any tool that passes a file list (an editor, a pre-commit hook) lints
# the vendored tree even though `ruff check src` is clean.
force-exclude = true
# Vendored third-party trees only. particle_filter comes back into scope: it is
# ours. pure_pursuit stays out -- it is xLab's code, not ours.
extend-exclude = [
  "src/hardware/f1tenth_system/**",
  "src/navigation/pure_pursuit/**",
  "src/perception/livox_sdk2/**",
  "src/perception/livox_ros_driver2/**",
  "src/perception/zed_wrapper/**",
  "src/simulation/f1tenth_gym/**",
  "src/simulation/f1tenth_gym_ros/**",
]

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
    # pydocstyle
    "D",
    # pep8-naming
    "N",
]
ignore = [
  # line-too-long since built-in formatter only does best effort
  "E501",
  # indentation-with-invalid-multiple
  "E111",
  # indentation-with-invalid-multiple-comment
  "E114",
  # over-indented
  "E117",
  # indent-with-spaces
  "D206",
  # triple-single-quotes
  "D300",
  # missing docstring in public module
  "D100",
  # missing docstring in public package ('__init__' file at root of a python package)
  "D104",
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.10"

# Build output and vendored third-party trees, matching ruff above. The one
# addition is setup.py: every ament_python package has one, they are all the
# top-level module "setup", and mypy refuses to check a tree with duplicate
# module names ("Duplicate module named 'setup'") rather than checking any of
# it. Ruff still lints these files.
exclude = [
    "^build/",
    "^install/",
    "^log/",
    ".*/setup\\.py$",
    ".*/build/",
    ".*/install/",
    ".*/log/",
    "^src/hardware/f1tenth_system/",
    "^src/navigation/pure_pursuit/",
    "^src/perception/(livox_sdk2|livox_ros_driver2|zed_wrapper)/",
    "^src/simulation/(f1tenth_gym|f1tenth_gym_ros)/",
]

warn_no_return = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
warn_unused_configs = true
warn_unused_ignores = true

disallow_untyped_defs = true
disallow_untyped_decorators = true

strict_equality = true
strict_concatenate = true

ignore_missing_imports = true
follow_imports = "skip"
