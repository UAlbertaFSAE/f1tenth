# Common
launch_pkg - This package launches all required nodes for full autonomous driving, consult someone before adding node to launchfile

rc_interfaces - Contains our custom Cone and Cones ros2 messages

### What goes here?
The `common/` folder should contain any ROS packages that are common to all other packages (like custom messages), as well as things that are just general to the stack.
